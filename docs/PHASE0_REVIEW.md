# ReactiveDynamics.jl — Phase-0 Review & Sign-off Summary

> **Status: SIGNED OFF 2026-06-20 (§1–§9 / ADRs 0001–0006).** The maintainer approved the contract (§1–§9) and ADRs 0001–0006 and resolved all remaining scoping calls (§5). Phase 1 implementation may begin. The §1–§9 baseline made no `src/` changes (contract-before-code); the **Phase-0.6 increment is the exception** — it landed as engine code (§9.2 below).
>
> **Phase-0.5 modeling-language extension — PROPOSED 2026-06-21 (§8 of this summary).** Three additive ADRs (0007–0009) and contract sections (§10, §11, §9.5) answer the maintainer's business-process modeling asks: a more refined/compact/compositional metalanguage with substitutable granularity, a contracted interface with declarative + dumpable initial state, and filtration-based selection of agentic-species tokens. (Extended the same day by ADRs 0010–0012 / contract §12, §13: the endogenous decision channel, action callbacks, and AlgebraicAgents integration.) Read-verified against `ref-agents`; awaiting sign-off and a running-engine verification pass.
>
> **Phase-0.6 analysis & visualization increment — IMPLEMENTED 2026-06-30 (§9 of this summary).** Two additive ADRs (0013–0014) and contract sections (§14, §15) answer the maintainer's result-inspection asks: inspect an ensemble of runs, gather ledger statistics, extract "typical" agentic-token trajectories, export results, plot via model-agnostic recipes, and draw a result-decorated network "exec map" for (in)efficiency analysis. The layer is read-only over a finished run and reuses existing infrastructure (the per-program-ledger seam; AA's `getobservable`/`run_graphviz`/`wiring_diagram`) rather than adding dependencies. **It is now landed as engine code** (commit `6174ebe` on `ref-agents`; AlgebraicAgents pinned to the published GitHub repo via `[sources]`) — the first `src/` change of the Phase-0 line. The five work packages, the files touched, and the green suite (500 pass / 7 broken / 507) are recorded in §9.2.

This is the single-page map of everything Phase-0 produced. The §1–§9 baseline was "contract before code": no production `src/` changes landed for it; that material is documentation and tests, and the engine was unchanged. The Phase-0.6 increment (§9.2) is the first exception — it landed as `src/` code. All artifacts were adversarially verified against the current `ref-agents` source (several verifier passes ran the engine on Julia 1.12.5), and every `file:line` citation was checked — re-verify before acting, as line numbers drift.

## 1. What Phase 0 decided (the short version)

ReactiveDynamics is a **timed, stochastic, resource-constrained Petri net / discrete-event system** for modeling business / R&D processes (budgeting, ledgers, what-if, rNPV) — not a chemical reaction network. The `ref-agents` branch had already pivoted to a native engine; Phase 0 ratified and specified that pivot rather than re-litigating it. Five decisions, all recorded as ADRs:

| ADR | Decision | Status |
|---|---|---|
| [0001](adr/0001-discrete-event-engine.md) | Keep the native discrete-event engine `ReactionNetworkProblem` (an AlgebraicAgents `@aagent` stepped via `_step!`); SciML demoted to an optional extension. | Accepted 2026-06-17 |
| [0002](adr/0002-priority-weighted-allocation.md) | Priority-weighted allocation via **weighted progressive filling** (water-filling) — work-conserving, conjunctive-consistent, deterministic, dependency-free; replaces the 7-function per-resource-split tangle. | Accepted 2026-06-17 |
| [0003](adr/0003-data-store.md) | **Drop ACSets** for a dependency-free typed-struct-of-columns IR; **promote** the transition↔reactant relation to a first-class typed `ReactantSpec` incidence table; no backward on-disk compatibility. | Accepted 2026-06-18 |
| [0004](adr/0004-runtime-mutation.md) | **Runtime mutation = append-only + soft-deactivate**, so transitions/species/params can be added (and transitions retired) *during* simulation without breaking the position-indexed compiled closures. | Accepted 2026-06-18 |
| [0005](adr/0005-serialization-json-ir.md) | **Single JSON serialization** + typed `ExprNode` IR — eval-free, JSON-Schema-describable for LLM emission/validation; closes the import-time RCE; drops the TOML/CSV/JLD2 zoo. | Accepted 2026-06-18 |
| [0006](adr/0006-structured-tokens.md) | **Structured/agentic tokens**: live instantiation/query (BD projects as entities), append-only-safe via `entangle!`; **custom-function registry replaces `@register`** (host-Julia vs serializable-data boundary, eval-free). | Accepted 2026-06-20 |
| [0007](adr/0007-interface-and-initial-state.md) | **Interface & initial-state contract**: three-phase lifecycle; declarative serializable initial marking `population[]` (structured analogue of `specInitVal`); state dump/restore (superset schema). | Proposed 2026-06-21 |
| [0008](adr/0008-token-filtration.md) | **Agentic species under a filtration**: `TokenPredicate{kind,clauses}` + `@select` — select tokens by 𝓕ₜ-measurable predicate, not kind alone; PHASE IS AN ATTRIBUTE canonically (per-KIND is the degenerate `species` predicate), with `@advance`/`SetField` the field-write that generalizes `@move`. | Proposed 2026-06-21 |
| [0009](adr/0009-refinement-and-composition.md) | **Refinement & open-port composition**: `refine`/`abstract` (substitutable granularity via the FK-splice), open ports, `@pipeline`/`@process`/`@compose` compact authoring. | Proposed 2026-06-21 |
| [0010](adr/0010-rules-and-conditional-transitions.md) | **Rules/triggers & conditional transitions**: the endogenous decision channel — repairs the no-op event channel into a `Rule{guard,action,fire_mode}`; adds a stateless transition `guard`; the in-model acquisition lever. | Accepted 2026-06-21 |
| [0011](adr/0011-action-callbacks-and-general-code.md) | **Action callbacks**: population-level token writes (`SetTokens` over a `TokenPredicate`) + the general-code escape hatch (`Invoke` via the eval-free registry). | Accepted 2026-06-21 |
| [0012](adr/0012-algebraicagents-integration.md) | **AlgebraicAgents integration**: RD as a hierarchy node (outbound `getobservable`/`observables`/params) + inbound external coupling (`inputs[]` ports, `ExternalRef` leaf, `_prestep!` latch, one-tick Jacobi lag). | Proposed 2026-06-21 |
| [0013](adr/0013-analysis-and-observability.md) | **Analysis & observability**: per-token trajectory log (orchestrator-driven `log_token_fields` hook + columnar store, generalizing the per-program ledger row); ensemble runner (`EnsembleProblem` as an AA node, `summarize`/`treatment_effect`); results export bundle (CSV/JSON core, Arrow weakdep). | Implemented 2026-06-30 (`6174ebe`) |
| [0014](adr/0014-visualization.md) | **Visualization**: model-agnostic result-plot recipes behind `RDPlotsExt` (+ removal of the dead SciML `@plot`); the three-layer network "exec map" (dep-free structure → Graphviz via AA's `run_graphviz`, composed with `wiring_diagram` → run/ensemble result overlay for bottlenecks + `@select` token highlighting). | Implemented 2026-06-30 (`6174ebe`) |

## 2. The modeling contract (`docs/CONTRACT_DRAFT.md`)

§1–§9 complete and signed off; §9.5 and §10–§13 are the Phase-0.5 extension increment (§12 accepted, the rest proposed). This is the normative specification the engine must satisfy.

| § | Section | Pins |
|---|---|---|
| 1 | Modality Truth Table | Orthogonal axes `allocation∈{upfront,perstep}` × `return∈{consumed,conserved}` × `blocking∈{block,nonblock}`; 5 legal rows + illegal combos; maps to the legacy `Set{Symbol}` and to ADR 0002's `build_requirements!`. |
| 2 | Time Model (+ §2.8 Genesis) | Single clock; Poisson spawn intensity; cycle/lifetime; dt-invariance hazards. §2.8: genesis is two-stage (rate proposal + upfront-LHS gate); closed `{poisson,scheduled,flow,capacity}` intent tag; source vs routing. |
| 3 | Operational Semantics | Instance lifecycle; the 13-step `_step!` tick; 7 contract invariants (non-negativity, conservation, capacity, integrality, determinism, termination-completeness, event-firing). |
| 4 | Determinism & Seeding | D1–D9: thread an `AbstractRNG`, per-trajectory seeding, reinit restores the stream. |
| 5 | Attribute Contract | Per-attribute units / range / default / time-varying policy; replaces the `SampleableValues` catch-all. |
| 6 | Object Model | Typed columnar tables under the append-only index invariant; the promoted `ReactantSpec` incidence table; identity-by-name; structured-token refinement. |
| 7 | Composition Semantics | Join (name-merge S/P/M, disjoint T) + the obs/`:E` merge gap; equalize; FK-repoint vs string-surgery; the `rem_parts!` live-guard; the undefined `include_model` bug. |
| 8 | Serialization Schema | Cross-references ADR 0005: round-trip + `(model, seed)`-determinism guarantees; eval-free `validate`; RCE closure; outputs→Arrow; the append-only mutation-patch form. |
| 9 | Structured Tokens & Queries | Cross-references ADR 0006: token instance lifecycle (instantiate/bind/move/unbind/retire), append-only-safety via `entangle!`, 7 invariants incl. D4 token total-order; the deterministic query API; the `TokenAgg` ExprNode; the host-Julia-vs-data boundary and the custom-function registry replacing `@register`. **§9.5 (Phase-0.5):** predicate selection & state advance of agentic tokens — the `TokenPredicate{kind,clauses}` node + `@select`, 𝓕ₜ-measurability, phase-as-attribute as canonical (per-KIND = degenerate `species` predicate), and the `SetField`/`@advance` field-write generalizing `@move` (ADR 0008). |
| 10 *(Phase-0.5)* | Interface & Initial-State | Cross-references ADR 0007: the three-phase lifecycle (authoring → construction → live) + legal-op table; the declarative serializable initial marking `population[]` (closes the §8.2 S2 hole for structured runs); state dump/restore as a superset schema (the maintainer's "list of structures" + "dump the system state"); the completed `reinit!`. |
| 11 *(Phase-0.5)* | Refinement & Open-Port Composition | Cross-references ADR 0009: ports as a Species `role` annotation; `refine`/`abstract` as the §7.4/J7 FK-splice (plug-compatible substitution of granularity); `@pipeline`/`@process`/`@compose` compact authoring; closes the §7/J4 (`:E`/`:obs`) and J9 (`include_model`) bugs. |
| 12 *(Phase-0.5)* | Rules, Triggers & Conditional Transitions | Cross-references ADR 0010/0011: the endogenous decision channel — the repaired `Rule{guard,action,fire_mode}` event channel + a stateless transition `guard`; the closed action family `{SetSpecies,SetParams,SetField,SetTokens,AddToken,Activate,Deactivate,Invoke,Log,Seq}` (declarative verbs + the eval-free `Invoke` escape hatch); fire-point/determinism placement. |
| 13 *(Phase-0.5)* | AlgebraicAgents Integration & External Coupling | Cross-references ADR 0012: RD as an AA hierarchy node — outbound `getobservable`/`observables`/`_getparameters`/`_setparameters!`; inbound `inputs[]` ports + the `ExternalRef` ExprNode leaf fed by AA wires; the `_prestep!` latch giving deterministic one-tick-lag (Jacobi) co-simulation; AA's least-projected-time clock coordination. |
| 14 *(Phase-0.6)* | Analysis & Observability | Cross-references ADR 0013: the per-token trajectory log (passive-token `log_token_fields` hook + columnar store at the `push_program_ledger_row!` seam, with `representative_token`/`trajectory_envelope` "typical" helpers); the ensemble runner (`ensemble`/`summarize`/`treatment_effect`, `EnsembleProblem` reusing the §13.2 read surface, mode-(b) gated on §10.4/ADR 0007 §D); the results export bundle (per-artifact CSV/JSON, Arrow weakdep) implementing the §8.5 outputs clause. |
| 15 *(Phase-0.6)* | Visualization | Cross-references ADR 0014: model-agnostic result-plot recipes behind `RDPlotsExt` (+ removal of the dead SciML `@plot`/`plot_df`); the three-layer network exec map — dep-free `network_graph` extraction, `to_graphviz`/`draw_network` reusing AA's `run_graphviz` and composing with `wiring_diagram`, the `exec_map` result overlay (bottleneck/starvation coloring + `@select` token-path highlighting); Petri net primary, AA tree as composed frame. |

## 3. The Phase-0 semantic test suite (`test/semantic/`)

**63 testsets** across 4 files, wired into `test/runtests.jl`, replacing the assertion-free tutorial smoke tests as the real acceptance layer. All files parse-clean on Julia 1.12.5.

Two tiers:
- **T1-characterization (39 tests)** — run against the *current* engine; they lock in correct behavior or **pin a known bug with `@test_broken`** so the suite is green-when-expected.
- **T2-acceptance (24 tests)** — encode the *target* behavior from the contract/ADRs; `@test_skip`-wrapped with a reference block because they name not-yet-built APIs (`progressive_fill!`, the `seed=` kwarg, `ReactantSpec` FK-repoint). Phase 1 flips each `@test_skip`/`@test_broken` → `@test` as the piece lands.

| File | Testsets | Covers |
|---|---|---|
| `allocation_conservation_lifecycle.jl` | 18 (T1=8, T2=10) | ADR 0002 allocation oracles; conservation & non-negativity (§3.4); instance lifecycle & cycle time. |
| `modality_genesis.jl` | 17 (T1=14, T2=3) | The 5 modality rows + illegal combos (§1); genesis modes poisson/scheduled/flow/capacity (§2.8). |
| `determinism_composition_bugs.jl` | 14 (T1=7, T2=7) | Seeding D1–D9 (§4); join/equalize (§7); the remaining bug-pins. |
| `reference_models.jl` | 14 (T1=10, T2=4) | SIR, toy-pharma, rNPV — the brief's headline acceptance criteria. |

## 4. Known engine bugs the suite pins (fix in Phase 1)

These survive on `ref-agents`; the contract documents them and the tests pin them so the regression is visible. (Re-verify line numbers.)

| Bug | Site | Effect |
|---|---|---|
| Undefined `q` in `free_blocked_species!` | `solvers.jl:512` | `:nonblock` resource-release path crashes whenever a `:nonblock` token is in-flight. |
| Lifetime-terminated instances never pruned | `solvers.jl:501` | `filter!` keeps `state < cycleTime`, so timeout-terminated instances re-emit RHS + re-return conserved tokens every subsequent tick — violates conservation. |
| `event_action!` is a no-op | `solvers.jl:323` | Fetches `:eventAction` but never evaluates it; events do not fire (so the acquisition lever must use the scheduled rate idiom, not events). |
| `add_to_spawn!` deferral broken | `state.jl:251-256` | `findfirst` gets a scalar, and it increments `:transHash` not `:transToSpawn` — capacity-overflow deferral is lost. |
| `resample!` writes nonexistent `o.val` | `state.jl:137` | Range-less observable crashes (field is `sampled`). |
| `@mode` bare `specModality` | `update.jl:108` | `UndefVarError` on any `@mode` call. |
| `ceil` breaks dt-invariance | `solvers.jl:144` | Fractional spawn counts round up every tick off the Poisson path. |
| No RNG seeding | `create.jl:151`, `solvers.jl:413,321`, `state.jl:123` | All draws use the global RNG; determinism-under-seed not achievable (the §4 gap). |
| `@join` file branch | `joins.jl:226,228` | Calls undefined `include_model`. |

## 5. Decisions needed from the maintainer

### 5a. Sign-off (the gate) — GRANTED 2026-06-20
The maintainer approved the contract (§1–§9) and ADRs 0001–0006 as the Phase-0 baseline, and accepted all four remaining scoping calls (5b). **Phase 0 is signed off; Phase 1 implementation may begin.** No `src/` changes were made during Phase 0.

### 5b. Scoping calls — RESOLVED 2026-06-20
- **ADR 0003 — Phase 3 interop adapter → DROPPED/deferred.** No `to_acset` weakdep view shipped; interop is off-roadmap and impossible for the running engine. Reversible later.
- **Structured-token determinism source (ADR 0006) → per-species creation counter.** The k-th token of a species gets a monotonic creation index; sort-key = `(species, creation_index)`. The uuid stays as identity; no RNG-threaded uuid generation. Counter is reset by `_reinit!`.
- **Retired-token growth (ADR 0006) → accept for Milestone 1 + escape hatch.** Soft-`:removed` is the default (preserves `past_bonds`); `disentangle!` is the opt-out; periodic archival only if a real run shows the O(#tokens) cost bites.
- **Genesis default (CONTRACT §2.8) → engine default `poisson`, template/authoring default `flow`.** Keeps existing models stable while steering BD/pipeline routing transitions to the correct semantics.

### 5c. Earlier-resolved (for the record)
- **ADR 0002:** priority is dynamic/time-varying; per-tick fairness; `priority=0`=leftover-only.
- **ADR 0003:** no on-disk back-compat; promote the reactant; single JSON.
- **`@register` / custom functions (ADR 0006).** Removed as a model-authoring/eval path; replaced by a per-network **registry** of host-supplied Julia functions referenced BY NAME (closed allow-list, eval-free) — this is why `@register` had to `@eval` into RD's module (the compiled closure's bare call-head, `compilers.jl:28,122`) and why the registry dissolves the need.
- **Action statements (ADR 0005/0006).** `{SetSpecies, SetParams, Log, Seq}` confirmed sufficient for now.
- **Resource retirement (ADR 0004 + 0006).** Species KINDS stay defined a priori (no live species-kind retirement needed); transitions retire live via `deactivate!`; structured-token INSTANCES are created and retired live (soft `:removed` / hard `disentangle!`). No `specActivated` flag is added. (A residual: a *plain* species can still only be orphaned, not retired, mid-run — accepted, since the live-retirement need is on structured-token instances, which ADR 0006 fully supports.)

## 6. Provenance

Phase-0 artifacts and commits on `ref-agents`: `b69cfb9` (inventory + ADR 0001/0002), `ebccf9f` (ADR 0003 + contract §1–§5), `3bf2e88` (accept 0003, add 0004/0005, genesis), `c3f6161` (contract §6–§8 + test suite), `4d2c109` (this review summary), `7867669` (ADR 0006 + contract §9), plus the sign-off commit recording the §5 resolutions. The ground-truth source map is `INVENTORY.md` (supersedes the stale architecture map in `REVIEW.md`). Workflows that produced these were adversarially verified; corrections from engine-run verifiers were applied (notably: `simulate(a, max_t)` takes a max-*time* and returns the agent; the `seed=` kwarg is currently swallowed; events are non-functional).

## 7. What Phase 1 looks like (after sign-off)

Implement the contract against the native engine: the typed IR + `const SCHEMA`; the weighted-progressive-filling allocator (`build_requirements!`/`progressive_fill!`/`spawn_integer!`); orthogonal typed modalities + construction-time validation; the promoted `ReactantSpec` table; an `AbstractRNG` + `seed=` threaded through every draw; the append-only live mutation API; the structured-token subsystem (the per-network function/kind registry replacing `@register`, the deterministic token query API + `token_sortkey` total order, the `Construct{kind,args}`/`TokenAgg` ExprNodes, and the token-binding bug fixes at `solvers.jl:288,452,455,476,512`); and the bug fixes in §4. Acceptance = the T2 tests flip green, SIR + toy-pharma reproduce known behavior under seed, and the BD projects-as-tokens scenario (ADR 0006 north-star) runs with the acquisition lever applied live.

## 8. Phase-0.5 — the modeling-language extension (proposed 2026-06-21)

After §1–§9 sign-off, the maintainer asked to refine the metalanguage for business processes: compact/expressive definition, compositionality, substitutable granularity; a contract for the interface methods (definition, initial values / initial state, simulation, joins); and handling of "agentic" species selected by a filtration expression. The increment is three ADRs (0007–0009) + three contract sections (§10, §11, §9.5), all ADDITIVE — they produce a plain `ModelSpec` that constructs/serializes/simulates exactly as a flat model, disturbing none of the signed-off §1–§9 semantics. The leverage point throughout is that the heavy mechanisms already exist: refinement reuses the §7.4/J7 FK-splice, predicate selection is a `filter` in front of the existing token sort, and the state dump is the initial-marking schema plus dynamic overlays.

| Ask (maintainer) | Answer | Where |
|---|---|---|
| Interface contract for definition / initial values / initial state / simulation / joins | Three-phase lifecycle (authoring → construction → live) + legal-op table; canonical signatures; the §7.5/J8 live-guard generalized to a phase guard. Joins were already contracted (§7); this places everything else. | ADR 0007 → §10.1–10.2, §10.4 |
| Initial STATE (not value) of agentic tokens — "instantiate as a list of structures and add them" | Declarative serializable `population[]` (count+attribute-dists OR explicit list of structs), instantiated at construction before t=0 with per-species creation indices through the seeded RNG; the structured analogue of `specInitVal`. Closes the §8.2 S2 reproducibility hole for structured runs. | ADR 0007 → §10.3 |
| "Serialization to dump the state of the system" | `dump_state`/`restore` checkpoint whose schema is the initial marking PLUS dynamic state (clock, RNG, creation counters, plain `u`, full token population with current field values, in-flight `ongoing`); eval-free (kinds/fields by name via the host registry). A zero-tick dump IS an initial marking. Enables halt/resume and same-seed lever A/B. | ADR 0007 → §10.5 |
| "Agentic" species under a filtration — "take a project in a given state subject to a filtration expression" | `TokenPredicate{kind, clauses}` ExprNode + `@select` LHS form; binding generalizes to `filter(kind && !isblocked && matches(pred))` before the unchanged sort/take; 𝓕ₜ-measurable (no future, no RNG). Phase is an attribute canonically — `get_species==kind` is the degenerate `species` clause; advancing state is `@advance`/`SetField` (the field-write that generalizes `@move`). | ADR 0008 → §9.5 |
| Compact/expressive, compositional, multi-granularity with refined dynamics substituted | Open ports (Species `role`); `refine`/`abstract` as the boundary-matched FK-splice (plug-compatible substitution); `@pipeline` chain sugar; `@process` reusable parameterized modules; `@compose` port-connected composition (closing the §7 J4/J9 bugs). | ADR 0009 → §11 |

**Dependency / sequencing.** ADR 0007 is lowest-risk and should land FIRST — it closes structured-run reproducibility and is a prerequisite for any structured Phase-1 work (the initial marking is how the token subsystem gets its t=0 population). ADR 0008 has the highest BD payoff and rides the ADR 0006 token machinery. ADR 0009 is gated on the ADR 0003 Phase-2 reactant promotion (the FK-splice it reuses). New acceptance tests (the initial-marking determinism + dump round-trip; the `@select` bind determinism + round-trip; the `refine` plug-compatibility + round-trip) are added to `test/semantic/` alongside their ADRs. Open questions are tracked per-ADR (the heaviest: serializing in-flight `ongoing` snapshots eval-free, ADR 0007; the predicate observation-point ratification, ADR 0008; refining a structured/predicated transition, ADR 0009).

## 9. Phase-0.6 — the analysis & visualization increment (proposed 2026-06-29, implemented 2026-06-30)

Beyond running a model, the maintainer asked to INSPECT the result: an ensemble of runs, statistics gathered from the ledger (valuations), "typical" trajectories for the agentic/stateful tokens, exports, plots — and graphical presentation of the reactive networks themselves (hierarchies, connections, wires; the ability to highlight tokens; a system "exec map" readable for (in)efficiencies, ultimately decorated with simulation results to show bottlenecks). The increment is two ADRs (0013–0014) + two contract sections (§14, §15), all ADDITIVE and READ-ONLY over a finished run — they touch none of the §1–§13 dynamics. The leverage point is again that the heavy mechanisms already exist: the per-token trajectory log generalizes the per-program-ledger row (same tick seam, same `token_sortkey` order); the ensemble result is an AlgebraicAgents node reusing the §13.2 read surface; and the network map reuses AA's `wiring_diagram`/`run_graphviz` so no graph dependency is added. The starting point is `@select` being live already (it drives every `@advance` transition in the BD pipeline model), so predicate-scoped logging/highlighting is the existing selection machinery.

| Ask (maintainer) | Answer | Where |
|---|---|---|
| Inspect the result of a run; gather statistics from the ledger | Already partly met — `prob.sol` (trajectory DataFrame), `prob.log` (event stream incl. `:valuation*`), `program_ledger(prob) → DataFrame` (per-program cost/reward/valuation). §14 adds the export and cross-run aggregation. | ADR 0013 → §14.1, §14.3 |
| "Typical" trajectories for the agentic/stateful tokens | Tokens are passive (`_step!`==nothing), so the ORCHESTRATOR logs them: a per-kind `log_token_fields` hook + a columnar `token_trajectory` store appended at the per-program-ledger seam (`solvers.jl:1037`), in `token_sortkey` order. "Typical" made precise as `representative_token` (medoid) + `trajectory_envelope` (median + IQR band). | ADR 0013 → §14.1 |
| Inspect an ENSEMBLE of runs (Monte-Carlo); statistics over them | `ensemble(build; nseed, root_seed)` (per-member `hash((root_seed,k))` seeding, §4 D8); `EnsembleProblem <: AbstractAlgebraicAgent` reuses the §13.2 read surface; `summarize`/`treatment_effect`. Built on AA's primitives, NOT by modifying AA. Member-reuse mode (b) gated on §10.4/ADR 0007 §D `_reinit!`. | ADR 0013 → §14.2 |
| Export (balance CSV simplicity vs JSON refinement) | Format per artifact: rectangular (`sol`/ledger/summary) → CSV + Arrow; heterogeneous (`log`/token trajectories/manifest) → JSON. `export_run`/`export_ensemble` write the §8.5 `runs/<model_hash>/<seed>/…` bundle; JSON core (reuses `serialize.jl`), CSV via the present dep, Arrow behind `RDArrowExt`. | ADR 0013 → §14.3 |
| Plot the results (default recipes; drop unused code) | A model-agnostic Plots.jl `@recipe` set behind `RDPlotsExt` keyed off raw artifacts (7 recipes: marking, saturation, valuation, ledger, token+envelope, ensemble/treatment-effect, throughput). The dead SciML `@plot`/`plot_summary` (`solve.jl`) and unused `plot_df` are removed. | ADR 0014 → §15.1 |
| Graphical presentation of the reactive networks — hierarchies, wires, token highlighting, an "exec map" for (in)efficiencies, integrated with results | Three layers: (A) dep-free `network_graph` structure extraction; (B) `to_graphviz`/`draw_network` via AA's `run_graphviz`, COMPOSED with `wiring_diagram` so the §13 coupling wires/hierarchy are on the same map; (C) `exec_map` result overlay — bottleneck/starvation coloring from `:saturation`/pool troughs, token-path highlighting from `past_bonds` scoped by `@select`. Petri net primary, AA tree as composed frame. | ADR 0014 → §15.2, §15.3 |

**Dependency / sequencing.** ADR 0013 §14.1 (the per-token trajectory log) should land FIRST — it is a single read-only tick step and unblocks both the "typical trajectory" analysis and the §15.1 recipe-5 (token plots) and §15.2 Layer-C (token highlighting). The §14.2 ensemble runner ships on rebuild-per-seed mode (a) immediately; member-reuse mode (b) flips on when ADR 0007 §D completes `_reinit!` (RNG/counter/population restore). §14.3 export and §15.1 recipes are independent and can land in parallel. §15.2 the network map is the largest piece but lowest-risk — Layer A is buildable on the current `transLHS`/`transRHS` incidence (it simplifies, not blocks, when the ADR 0003 `ReactantSpec` promotion lands), Layer B reuses AA's Graphviz machinery, and Layer C just consumes §14 statistics. New acceptance tests (trajectory-log determinism + round-trip; ensemble seed-determinism + `treatment_effect` reproduction; export round-trip; `network_graph` round-trip + valid-DOT emission for the reference models) are added to `test/semantic/` alongside their ADRs. Open questions are tracked per-ADR (the heaviest: trajectory-log cardinality/streaming and ensemble member retention at scale, ADR 0013; exec-map layout at scale + overlay color semantics, ADR 0014).

### 9.1 Implementation plan — build order, parallel tracks, and seams

The sequencing above resolves into five work packages (WP) over four waves. The spine is the maintainer's stated order — **token log → ensemble mode (a) now / mode (b) after ADR 0007 §D → export ‖ recipes → network map last** — with every package that carries no upstream data dependency pulled forward to run concurrently. The critical path is deliberately short: only the token log (WP-1) gates downstream work (recipe-5, exec-map Layer C, the ensemble cohort band). Everything else is either a dependency-free enabler or fans out once its inputs exist, so the wall-clock shape is "token log + enablers together → export/recipes/ensemble fan out → network map closes."

| Wave | WP | Scope (§) | Hard deps | Parallel with | Exit gate |
|---|---|---|---|---|---|
| 0 | **WP-0a** extension scaffolding | §14.3/§15.1 plumbing | — | all of Wave 1 | `Project.toml` gains `[weakdeps]`+`[extensions]`; skeleton `ext/RDArrowExt.jl`+`ext/RDPlotsExt.jl` load-clean |
| 0 | **WP-0b** delete dead SciML viz | §15.1 | — | everything | `solve.jl:7-201` + `plots.jl:3-10` removed; `@agentize`/`_draw` kept; suite stays green |
| 1 | **WP-1** per-token trajectory log | §14.1 | — | WP-0a/b, WP-2 | trajectory-log determinism + round-trip test flips green |
| 2 | **WP-2** ensemble runner, mode (a) | §14.2 | — (scalar) ‖ WP-1 (cohort band) | WP-1 | ensemble seed-determinism + `treatment_effect` reproduction green; mode (b) `@test_skip` pending ADR 0007 §D |
| 2 | **WP-3** export bundle | §14.3 | WP-0a, WP-1, WP-2 | WP-4 | `export_run`/`export_ensemble` round-trip green (JSON/CSV core; Arrow behind weakdep) |
| 2 | **WP-4** result-plot recipes | §15.1 | WP-0a; recipe-5←WP-1, recipe-6←WP-2 | WP-3 | 7 recipe smoke tests green; BD figures collapse onto recipes 2/6 |
| 3 | **WP-5** network exec map | §15.2/§15.3 | Layer C←WP-1 (+WP-2) | — | `network_graph` round-trip + valid-DOT for SIR/toy-pharma green |

**Three cross-cutting facts found while grounding this plan (re-verify before acting — line numbers and package hashes drift).**
1. **No extension mechanism exists yet.** `Project.toml` carries a `[deps]` block only — there is no `[weakdeps]`/`[extensions]` stanza and no `ext/` directory. Both `RDArrowExt` (§14.3) and `RDPlotsExt` (§15.1) presuppose the Julia package-extension mechanism, so **WP-0a** (adding the two stanzas + the `ext/` skeleton) is a shared Wave-0 prerequisite, NOT part of either feature. It is dependency-free and small, so it parallelizes with the token log.
2. **The AlgebraicAgents source — RESOLVED by pinning to GitHub.** Per the maintainer, AA is now pulled directly from the published repo (`Merck/AlgebraicAgents.jl`, `rev = main`) via a declarative `[sources]` block in `Project.toml` (Julia 1.11+); `Pkg.resolve` writes the exact tree-sha into the Manifest. The git source resolves to slug `ovDs5` (v0.3.27) — which IS the slug the ADRs cite, so the §15.2 reuse points (`wiring_diagram` `wires.jl:144`, `run_graphviz` `graphviz.jl:18`) are valid as written (an earlier note that the source had "moved to kOuEk" was backwards — `kOuEk` was the older registry copy). Switching to the git source left the suite green (425/7).
3. **Ensemble mode (a) has no dependency on the token log.** Its scalar statistics (`summarize`/`treatment_effect`) read only `program_ledger`/`prob.sol`, which already exist, so **WP-2 can begin in Wave 1 alongside WP-1**; only the cross-member cohort band (`trajectory_envelope` lifted across members) waits for WP-1. The maintainer's "ensemble after token log" ordering is a risk/size preference, not a data dependency — so it is pulled forward (with the cohort-band stat sequenced behind WP-1).

**WP-1 — per-token trajectory log (§14.1, the keystone).** Add `token_trajectory::Vector{Tuple}` as a sibling of `log` (`state.jl:80`); the `log_token_fields(tok)::NamedTuple` generic (default `(;)`) lands host-side beside the kind structs (`interface/agents.jl`); `push_token_trajectory_row!(state)` is inserted into `_step!` between `push_program_ledger_row!` (`solvers.jl:1037`) and the clock advance (`solvers.jl:1039`), iterating in `select_tokens`/`token_sortkey` order (`predicates.jl:71,105`) exactly as `push_program_ledger_row!` does (`ledger.jl:185-189`); the read API `token_trajectory(state[, name|pred])` reuses the LIVE `TokenPredicate`/`@select` machinery (`predicates.jl:17,71`). Stage 1b — `representative_token`/`trajectory_envelope` — is pure post-processing over the store (new `src/analysis.jl`). Smallest slice, highest leverage: it is the sole hard prerequisite for recipe-5, exec-map Layer C, and the ensemble cohort band.

**WP-2 — ensemble runner, mode (a) (§14.2).** Promote `demo/bd_acquisition/analysis.jl`'s `ensemble`/`mean_rnpv`/`sem_rnpv`/`treatment_effect` (the `hash((root_seed,k))` per-member seeding, §4 D8, is already correct there) into `src/analysis.jl` as `ensemble(build; nseed, root_seed, max_t, parallel) → EnsembleProblem`, `summarize`, `treatment_effect`. `EnsembleProblem <: AbstractAlgebraicAgent` implements the §13.2 read surface (`observables`/`getobservable`) so it is itself a drawable node. Mode (a) (rebuild-per-seed) ships now via the §10.3 declarative `population[]`; mode (b) (reinit-reseed) stays `@test_skip` until ADR 0007 §D restores RNG/counters/population (`state.jl:90-105` documents the target, not today's `_reinit!`). A `log` line records which mode ran so a coupled reinit is never silently assumed.

**WP-3 — export bundle (§14.3).** `export_run`/`export_ensemble` in a new `src/export.jl`: the JSON core reuses `serialize.jl`'s Dict encoding for the non-rectangular streams (`events.json`/`tokens.json`/`run.json`/`ensemble.json`); CSV via the present `CSV.jl` dep (`trajectory.csv`/`ledger.csv`); Arrow behind `ext/RDArrowExt.jl` (WP-0a). The demo's dep-free manual JSON in `export_data.jl` is the proof-of-shape the engine writer replaces. Layout = the §8.5 `runs/<model_hash>/<seed>/…`. The JSON/CSV core can land before the Arrow weakdep, so WP-3 is not fully blocked on WP-0a — only its Arrow tier is.

**WP-4 — result-plot recipes (§15.1).** Seven `@recipe`s in `ext/RDPlotsExt.jl` (WP-0a), keyed off raw artifacts: recipes 1–4 and 7 stand alone on `prob.sol`/`prob.log`/`program_ledger`; recipe 5 (token + envelope) needs WP-1; recipe 6 (ensemble / treatment-effect) needs WP-2. The four hand-rolled BD figures (`figures.jl`) collapse onto recipes 6 (waterfall + synergy decomp + rNPV histogram) and 2 (capital tradeoff). The dead-code deletion is split out as **WP-0b** and pulled to Wave 0 because it is zero-dependency (`solve.jl:7-201` — the `EnsembleSummary`/`EnsembleSolution`/`@plot` orphans — and the never-called `plot_df`, `plots.jl:3-10`; keep `@agentize` at `solve.jl:1` and `_draw` at `plots.jl:13`).

**WP-5 — network exec map (§15.2/§15.3, last).** Layer A `network_graph(prob_or_spec) → NetworkGraph` walks the current `transLHS`/`transRHS` incidence (`state.jl:306-307`, `solvers.jl:662`), dependency-free and no run needed — buildable today, simplifying (not blocking) when the ADR 0003 `ReactantSpec` table lands. Layer B `to_graphviz`/`draw_network` emits Petri-net DOT rendered through AA's `run_graphviz` and composes with `wiring_diagram` as a cluster — the AA seams are valid against the pinned GitHub source (`ovDs5`, finding 2). Layer C `exec_map` overlays WP-1 `token_trajectory` (path highlighting via `@select`/`past_bonds`, `agents.jl:141`), WP-2 ensemble aggregates, and the existing `:saturation` row (`solvers.jl:444`) for bottleneck/starvation coloring — pure styling over §14 data, no new computation. Layers A/B stand alone; only Layer C needs the upstream WPs, which is why the map lands last with no idle waiting.

**Tests & exit criteria.** Each WP carries its acceptance tests into `test/semantic/` under the existing T1/T2 tier scheme (new `analysis_observability.jl` for §14, `visualization.jl` for §15), flipping `@test_skip`/`@test_broken` → `@test` as the piece lands: trajectory-log determinism + round-trip (WP-1); ensemble seed-determinism + `treatment_effect` reproduction of the BD Δ-rNPV (WP-2, with the mode-(b) reinit test skipped pending ADR 0007 §D); `export_run`/`export_ensemble` round-trip for the JSON/CSV/Arrow streams (WP-3, the §8 round-trip guarantee extended to outputs); seven recipe smoke tests that each consume a documented artifact with no BD-specific field (WP-4); `network_graph` round-trip from JSON + valid-DOT emission for SIR and toy-pharma (WP-5). Phase-0.6 is DONE when all five WPs' tests are green, mode (b) is the only remaining `@test_skip` (carrying its ADR 0007 §D gate note), and `demo/bd_acquisition`'s `analysis.jl`/`figures.jl`/`export_data.jl` are re-expressed as thin styling layers over the engine APIs — closing the run → statistics → export → plot → map loop the increment promises.

### 9.2 Implementation status — LANDED 2026-06-30 (ref-agents)

All five work packages are implemented against the native engine, plus the AA-from-GitHub pin. This is the first `src/` change of the Phase-0 line — the increment is no longer docs-only.

| WP | What landed | Files | Tests |
|---|---|---|---|
| AA pin | AA pulled from `Merck/AlgebraicAgents.jl@main` via `[sources]`; Manifest records the git tree-sha (`ovDs5`, v0.3.27) | `Project.toml`, `Manifest.toml` | baseline suite green (425/7) on the git source |
| WP-0a | `[weakdeps]`+`[extensions]` for `Arrow`/`Plots`; `Plots` demoted `[deps]`→`[weakdeps]`; `ext/RDArrowExt.jl`, `ext/RDPlotsExt.jl` | `Project.toml`, `ext/` | both extensions precompile clean |
| WP-0b | dead SciML plotting removed (`solve.jl` → just `export @agentize`; `plot_df` gone) | `src/interface/solve.jl`, `src/interface/plots.jl` | suite stays green |
| WP-1 | `token_trajectory` state field + reset in `_reinit!`; `log_token_fields` hook; `push_token_trajectory_row!` tick step after `push_program_ledger_row!`; `token_trajectory`/`representative_token`/`trajectory_envelope` | `src/state.jl`, `src/solvers.jl`, `src/interface/agents.jl`, `src/analysis.jl` | §14.1 determinism + reinit-reset + predicate-scope green |
| WP-2 | `ensemble`/`summarize`/`treatment_effect`; `EnsembleProblem <: AbstractAlgebraicAgent` with `observables`/`getobservable` | `src/analysis.jl` | §14.2 seed-determinism + reproduction + AA-node green; mode (b) NOT implemented (gated on ADR 0007 §D) |
| WP-3 | `export_run`/`export_ensemble`; JSON+CSV core always, Arrow via `_arrow_write` weakdep seam | `src/export.jl`, `ext/RDArrowExt.jl` | §14.3 JSON round-trip + Arrow-sibling green |
| WP-4 | 7 recipes + `_draw` relocated to the extension; thin wrapper types in core | `src/analysis.jl`, `ext/RDPlotsExt.jl` | all 7 recipes + `_draw` render green |
| WP-5 | Layer A `network_graph`/`NetworkGraph`; Layer B `to_graphviz`/`draw_network`; Layer C `exec_map` | `src/visualize.jl` | §15 structure + valid-DOT (SIR/toy-pharma) + RNG-purity + read-only green |

New test files `test/semantic/analysis_observability.jl` (§14) and `test/semantic/visualization.jl` (§15) are wired into `test/semantic/runtests.jl`; `test/Project.toml` gains `Arrow` so the weakdep paths are covered rather than skipped. The full suite after the increment is **500 pass / 7 broken / 507 total** (`Pkg.test()`, Julia, AA from the git source) — the +75 new §14/§15 assertions over the 425/7 baseline, with the 7 broken unchanged (the pre-existing engine bug-pins of §4) and zero regressions.

**Implementation notes (gotchas worth keeping).** (1) The extension `_arrow_write` MUST be an empty generic in core (`function _arrow_write end`) with the SOLE method in `RDArrowExt` — a core default *body* triggers "method overwriting during precompilation"; `export.jl` gates the call on `_arrow_available() = !isempty(methods(_arrow_write))`. (2) `@register`'d token kinds live in the `ReactiveDynamics` module, so a `log_token_fields`/population reference outside the `@register` block must be `RD.`-qualified. (3) The trajectory log stores `(t, name, species, fields)` and the determinism test compares FIELD VALUES (`select(df, Not(:program))`) because the demo's token names are `randstring`-based by design (§4 D4 is about field values, not names). (4) `network_graph` runs `sample_transitions!` on a `deepcopy` so it never perturbs the caller's `state.rng` (Layer A purity, Invariant 1). (5) A reactant's `species` can be an `Expr` (structured/parameterized product), so the `NetworkGraph` arc normalizes via `_species_sym`. (6) Demoting `Plots` to a weakdep means the main project no longer resolves it — the recipes/`_draw` exercise only in the TEST env (which declares `Plots`+`Arrow`); a bare `using ReactiveDynamics` loads neither extension, which is the intended lean-core behavior.

**Review pass (3 bugs found by adversarial review, all fixed + regression-tested).** (1) `EnsembleProblem` collapsed to ONE child — members built by the same closure all carry the default name `"reaction_network"`, and `entangle!` keys `inners` by name, so each overwrote the last; the per-run stats read the `members` vector so tests passed, but the "drawable/walkable AA node" invariant (§14.2 Inv 4) was silently false. Fixed by renaming each member `member_<k>` before `entangle!`; asserted `length(inners(ens)) == nseed`. (2) `exec_map` token-path highlighting never matched — it built the arc target from the bond's per-instance transition name (`"<name>_@<t>"`) while the graph nodes use `transName`; fixed by mapping the bond's transition INDEX (`Transition.i`) through the shared `_transition_node_name`, and asserted highlighted arcs match real graph arcs. (3) `log_count_series` read `:terminated_*` as all-zeros — those rows splat `Symbol=>Float64` **Pairs** (a `Pair` is not `<: Tuple`), so recipe 7's terminations were always 0; fixed to accept `Pair`/`Tuple`, asserted non-zero on the cycletime-1 pharma model. Also: `@advance` RHS arcs now resolve back to the structured LHS place (were pointing at a synthetic node named after the macro text), and the `run.json` manifest `artifacts` list now reflects what was actually written (incl. the Arrow siblings) instead of a hardcoded list.

**Demo collapse — DONE 2026-07-01.** The BD demo's `analysis.jl`/`run_demo.jl`/`figures.jl`/`export_data.jl` now delegate the ensemble fan-out, the cross-run `summarize`, and the unpaired `treatment_effect` to the engine APIs (`RD.ensemble`/`RD.summarize`/`RD.treatment_effect`); each scenario is an `EnsembleProblem`. Only genuinely demo-specific modeling remains hand-written (the rNPV discounting roll-up, the per-program-ledger views, and netting the acquisition price into the rNPV metric — the engine's symmetric `treatment_effect` does not model an asymmetric price). Verified numerically faithful: old-code vs collapsed-code at identical seeds agree to 9e-13 on every scenario's mean/Δ/SE. The demo gained its own `Project.toml` (path-`dev` RD + `Plots`/`Arrow`) because the Phase-0.6 weakdep demotion means `--project=.` no longer resolves `Plots` — so `figures.jl` had in fact been silently un-runnable since the demotion (it is not in the test suite); the demo env fixes that. NB the committed `presentation_data.json`/`figures/` (dated 2026-06-26) PREDATE the ADR-0002 allocator (2026-06-29, now live), so regenerating them shifts the numbers — a deck re-baseline left as a separate, explicit decision, not folded into the collapse.

**Still open / out of scope (unchanged from the ADRs).** Ensemble mode (b) (reinit-reseed) remains gated on ADR 0007 §D `_reinit!`. The `exec_map`/`draw_network` rendering is best-effort on the Graphviz backend (returns DOT always; renders when `Graphviz_jll`/system `dot` is present — present here).
