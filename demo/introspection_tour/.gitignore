# Everything under output/ is REGENERATED by introspection_tour.jl on each run
# (the rendered exec map + recipe PNGs, the DOT sources, and the export_run /
# export_ensemble bundles). It is a run artifact, not source — do not commit it.
output/
