# The resolved demo env is regenerated by `Pkg.instantiate()` (the repo root already ignores
# Manifest.toml globally; kept here for clarity).
Manifest.toml
# The two committed HTML outputs (wires_viz_tour_pluto.html, wires_literate.html) are the point of
# this demo — the maintainer compares them — so they are NOT ignored. Rebuild them with build.jl.
